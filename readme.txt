1.config下的accounts.py里面需要配置自己的京东账号
2.在testData下的测试数据.xlsx的文件里也有几个地方需要改成你的用户名、密码和用户pin（类似与一个唯一的id，和用户名不同）
3.运行：找到testScripts下的RunTests.py，右击运行即可。运行完成后会生成测试报告（test.html），并发邮件
4.在util下的SendMail.py里面需要设置你自己的邮箱信息：
